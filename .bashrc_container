
# Container-specific configuration
export DISTROBOX_CONTAINER="opencode-dev"
export PS1="[\u@opencode-dev \W]\$ "

# Skip zsh launcher when in container
# (The zsh exec is disabled above for distrobox)

# Reload environment
if [ -f ~/.bashrc ]; then
    source ~/.bashrc 2>/dev/null || true
fi
